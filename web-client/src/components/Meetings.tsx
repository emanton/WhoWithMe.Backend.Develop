import React, { useEffect, useState } from 'react'
import api from '../api'

export default function Meetings(){
  // delegate to pages/Meetings.tsx - this component is intentionally left small
  return null
}
